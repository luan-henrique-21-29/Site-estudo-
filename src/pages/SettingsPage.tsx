import { useRef, useState } from 'react'
import { Download, RotateCcw, Save, Upload, UserRound } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useAppState } from '../hooks/useAppState'
import { defaultSettings, exportData } from '../lib/storage'
import type { AppData, UserSettings } from '../types'

const themes = [
  ['Midnight','#6d5dfc','#6aa8ff','#ff5d8f','#0b1020','#151d33','#f6f7fb'],['Ocean','#2087ff','#72c4ff','#ff6b9e','#07182b','#10243d','#f4f9ff'],['Crimson','#e83a5f','#ff7b54','#ffb347','#0c0b10','#1b1721','#fff7f8'],['Purple Night','#9b5de5','#5f6fff','#f15bb5','#100b1e','#1d1532','#fbf8ff'],['Sunset','#ff6b4a','#a85cff','#ffb84d','#16101b','#28182f','#fff8f2'],['Clean','#4263eb','#7c8cff','#e64980','#f4f6fb','#ffffff','#182033'],['AMOLED','#7b61ff','#3b82f6','#ff4d8d','#000000','#0b0b10','#ffffff'],['Ice','#4c7dff','#91b7ff','#c063ff','#eef4ff','#ffffff','#152238'],['Neon Night','#6c63ff','#1f9dff','#ff3fa4','#05060d','#111529','#f5f8ff']
] as const
const uiFonts=['Inter, system-ui, sans-serif','Poppins, system-ui, sans-serif','Roboto, system-ui, sans-serif','Montserrat, system-ui, sans-serif','Nunito, system-ui, sans-serif','Open Sans, system-ui, sans-serif','Lato, system-ui, sans-serif','Source Sans 3, system-ui, sans-serif','system-ui, sans-serif']
const codeFonts=['ui-monospace, SFMono-Regular, Menlo, Consolas, monospace','Consolas, ui-monospace, monospace','Menlo, ui-monospace, monospace','monospace']
const customKey='futuro-lab-custom-themes-v1'
type ColorKey='primary'|'secondary'|'accent'|'background'|'surface'|'text'
type SavedTheme=Pick<UserSettings,'primary'|'secondary'|'accent'|'background'|'surface'|'text'>&{name:string}
function hexToHsl(hex:string){const raw=hex.replace('#','');if(raw.length!==6)return{h:0,s:0};const r=parseInt(raw.slice(0,2),16)/255,g=parseInt(raw.slice(2,4),16)/255,b=parseInt(raw.slice(4,6),16)/255;const max=Math.max(r,g,b),min=Math.min(r,g,b),d=max-min;let h=0;if(d){if(max===r)h=((g-b)/d)%6;else if(max===g)h=(b-r)/d+2;else h=(r-g)/d+4;h*=60;if(h<0)h+=360}const l=(max+min)/2;const s=d===0?0:d/(1-Math.abs(2*l-1));return{h,s}}
function isGreenish(hex:string){const{h,s}=hexToHsl(hex);return s>.18&&h>=70&&h<=170}
function readSavedThemes():SavedTheme[]{try{return JSON.parse(localStorage.getItem(customKey)??'[]') as SavedTheme[]}catch{return[]}}

export function SettingsPage(){
 const {data,updateSettings,replaceData,resetData}=useAppState();const fileRef=useRef<HTMLInputElement>(null);const[savedThemes,setSavedThemes]=useState<SavedTheme[]>(readSavedThemes);const[warning,setWarning]=useState('');const s=data.settings
 const applyTheme=(t:typeof themes[number])=>updateSettings({themeName:t[0],primary:t[1],secondary:t[2],accent:t[3],background:t[4],surface:t[5],text:t[6]})
 const setColor=(key:ColorKey,value:string)=>{if((key==='primary'||key==='secondary'||key==='accent')&&isGreenish(value)){setWarning('Verde continua bloqueado nas cores principais e destaques.');return}setWarning('');updateSettings({themeName:'Custom',[key]:value} as Partial<UserSettings>)}
 const duplicate=()=>{const item:SavedTheme={name:`Meu tema ${savedThemes.length+1}`,primary:s.primary,secondary:s.secondary,accent:s.accent,background:s.background,surface:s.surface,text:s.text};const next=[...savedThemes,item];setSavedThemes(next);localStorage.setItem(customKey,JSON.stringify(next))}
 const importFile=async(file?:File)=>{if(!file)return;try{const parsed=JSON.parse(await file.text()) as AppData;if(!parsed.settings||!parsed.goals)throw new Error();replaceData(parsed)}catch{alert('Backup inválido.')}}
 return <div className="page settings-page"><section className="page-header glass-panel"><div className="mega-icon">🎨</div><div><span className="eyebrow">Personalização</span><h1>Deixe com a sua cara.</h1><p>O básico fica na frente. Ajustes minuciosos continuam disponíveis sem transformar a página em painel de avião.</p><Link className="text-link" to="/profile"><UserRound size={16}/> Perfil e preferências de estudo</Link></div></section>
 <section className="glass-panel content-panel settings-quick"><span className="eyebrow">Rápido</span><h2>O que você realmente muda mais</h2><div className="quick-settings-grid"><label>Tema<select value={s.themeName} onChange={e=>{const found=themes.find(t=>t[0]===e.target.value);if(found)applyTheme(found)}}>{themes.map(t=><option key={t[0]}>{t[0]}</option>)}<option value="Custom">Custom</option></select></label><label>Fonte<select value={s.fontFamily} onChange={e=>updateSettings({fontFamily:e.target.value})}>{uiFonts.map(x=><option key={x} value={x}>{x.split(',')[0]}</option>)}</select></label><label>Tamanho: {Math.round(s.fontScale*100)}%<input type="range" min="0.8" max="1.5" step="0.05" value={s.fontScale} onChange={e=>updateSettings({fontScale:Number(e.target.value)})}/></label><label>Animação<select value={s.animations} onChange={e=>updateSettings({animations:e.target.value as UserSettings['animations']})}><option value="off">Desligada</option><option value="soft">Suave</option><option value="normal">Normal</option><option value="more">Mais animada</option></select></label></div><div className="theme-grid compact-theme-grid">{themes.map(t=><button key={t[0]} className={s.themeName===t[0]?'theme-card selected':'theme-card'} onClick={()=>applyTheme(t)}><span className="theme-preview" style={{background:`linear-gradient(135deg,${t[1]},${t[2]},${t[3]})`}}/><strong>{t[0]}</strong></button>)}</div></section>
 <details className="glass-panel advanced-settings"><summary>⚙️ Ajustes avançados</summary><div className="advanced-settings-body"><div className="split-grid"><section className="content-panel"><div className="section-heading"><h2>Cores</h2><button className="secondary-button" onClick={duplicate}><Save size={16}/> Salvar tema</button></div>{([['Principal','primary'],['Secundária','secondary'],['Destaque','accent'],['Fundo','background'],['Cards','surface'],['Texto','text']] as [string,ColorKey][]).map(([label,key])=><label className="color-row" key={key}><span>{label}</span><div className="color-value"><code>{s[key]}</code><input aria-label={`Cor ${label}`} type="color" value={s[key]} onChange={e=>setColor(key,e.target.value)}/></div></label>)}{warning&&<div className="warning-box">🟠 {warning}</div>}{savedThemes.length>0&&<div className="chip-row">{savedThemes.map((theme,index)=><button className="chip" key={`${theme.name}-${index}`} onClick={()=>updateSettings({...theme,themeName:theme.name})}>{theme.name}</button>)}</div>}</section><section className="content-panel"><h2>Tipografia</h2><label>Fonte dos títulos<select value={s.titleFontFamily} onChange={e=>updateSettings({titleFontFamily:e.target.value})}>{uiFonts.map(x=><option key={x} value={x}>{x.split(',')[0]}</option>)}</select></label><label>Fonte de código<select value={s.codeFontFamily} onChange={e=>updateSettings({codeFontFamily:e.target.value})}>{codeFonts.map(x=><option key={x} value={x}>{x.split(',')[0]}</option>)}</select></label><label>Títulos: {Math.round(s.headingScale*100)}%<input type="range" min="0.85" max="1.35" step="0.05" value={s.headingScale} onChange={e=>updateSettings({headingScale:Number(e.target.value)})}/></label><label>Código: {Math.round(s.codeScale*100)}%<input type="range" min="0.8" max="1.5" step="0.05" value={s.codeScale} onChange={e=>updateSettings({codeScale:Number(e.target.value)})}/></label><label>Altura de linha: {s.lineHeight.toFixed(1)}<input type="range" min="1.3" max="2" step="0.1" value={s.lineHeight} onChange={e=>updateSettings({lineHeight:Number(e.target.value)})}/></label></section></div><div className="split-grid"><section className="content-panel"><h2>Cards e espaço</h2><label>Escala dos cards: {Math.round(s.cardScale*100)}%<input type="range" min="0.9" max="1.15" step="0.05" value={s.cardScale} onChange={e=>updateSettings({cardScale:Number(e.target.value)})}/></label><label>Bordas: {s.radius}px<input type="range" min="0" max="32" value={s.radius} onChange={e=>updateSettings({radius:Number(e.target.value)})}/></label><label>Densidade<select value={s.density} onChange={e=>updateSettings({density:e.target.value as UserSettings['density']})}><option value="compact">Compacta</option><option value="normal">Normal</option><option value="spacious">Espaçosa</option></select></label><label>Sombras<select value={s.shadow} onChange={e=>updateSettings({shadow:e.target.value as UserSettings['shadow']})}><option value="off">Desligadas</option><option value="soft">Suaves</option><option value="medium">Médias</option></select></label></section><section className="content-panel"><h2>Fundo e rotina</h2><label>Fundo<select value={s.backgroundMode} onChange={e=>updateSettings({backgroundMode:e.target.value as UserSettings['backgroundMode']})}><option value="simple">Simples</option><option value="gradient">Gradiente</option><option value="particles">Partículas</option><option value="stars">Estrelas</option><option value="geometry">Geometria</option></select></label><label>Tempo padrão<select value={s.defaultStudyMinutes} onChange={e=>updateSettings({defaultStudyMinutes:Number(e.target.value)})}>{[5,10,20,30,45,60].map(x=><option key={x} value={x}>{x} minutos</option>)}</select></label><label className="toggle-row"><input type="checkbox" checked={s.sounds} onChange={e=>updateSettings({sounds:e.target.checked})}/><span>Sons de conclusão</span></label><small>Se o sistema pedir menos movimento (`prefers-reduced-motion`), essa preferência sempre vence.</small></section></div></div></details>
 <section className="glass-panel content-panel"><h2>Backup e dados</h2><div className="button-row"><button className="secondary-button" onClick={()=>exportData(data)}><Download size={18}/> Exportar backup</button><button className="secondary-button" onClick={()=>fileRef.current?.click()}><Upload size={18}/> Importar backup</button><input hidden ref={fileRef} type="file" accept="application/json" onChange={e=>importFile(e.target.files?.[0])}/><button className="secondary-button" onClick={()=>updateSettings({...defaultSettings})}><RotateCcw size={18}/> Restaurar aparência</button><button className="danger-button" onClick={()=>confirm('Resetar todo o progresso neste navegador? Faça backup antes se quiser guardar seus dados.')&&resetData()}><RotateCcw size={18}/> Resetar tudo</button></div><Link className="text-link" to="/privacy">Como meus dados são salvos? →</Link></section></div>
}
