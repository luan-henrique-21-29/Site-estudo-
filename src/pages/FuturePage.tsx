import { useState } from 'react'
import { Plus, Target } from 'lucide-react'
import { useAppState } from '../hooks/useAppState'

export function FuturePage(){
 const {data,updateGoal,addGoal}=useAppState(); const [title,setTitle]=useState(''); const [target,setTarget]=useState(100)
 return <div className="page"><section className="page-header glass-panel"><Target className="mega-icon-svg"/><div><span className="eyebrow">Planejamento pessoal</span><h1>Meu Futuro</h1><p>Transforme objetivos grandes em progresso visível.</p></div></section><div className="goal-grid">{data.goals.map(g=>{const pct=Math.min(100,Math.round(g.current/g.target*100));return <article className="goal-card glass-panel" key={g.id}><div className="card-top"><span className="pill">Meta</span><strong>{pct}%</strong></div><h3>{g.title}</h3><div className="progress-line"><span style={{width:`${pct}%`}}/></div><div className="goal-numbers"><span>{g.unit==='R$'?`R$ ${g.current.toLocaleString('pt-BR')}`:`${g.current}${g.unit}`}</span><span>de {g.unit==='R$'?`R$ ${g.target.toLocaleString('pt-BR')}`:`${g.target}${g.unit}`}</span></div><label>Atualizar progresso<input type="number" value={g.current} onChange={e=>updateGoal({...g,current:Number(e.target.value)})}/></label></article>})}</div><section className="glass-panel content-panel"><h2>Adicionar meta</h2><div className="form-row"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Ex.: Criar 10 projetos"/><input type="number" value={target} onChange={e=>setTarget(Number(e.target.value))}/><button className="primary-button" onClick={()=>{if(!title.trim())return;addGoal({id:`goal-${Date.now()}`,title:title.trim(),target,current:0,unit:'%'});setTitle('')}}><Plus size={18}/> Adicionar</button></div><h3>Checklist para morar fora</h3><div className="checklist-grid">{['Inglês','Dinheiro','País','Passaporte','Curso','Visto','Documentos','Trabalho','Moradia','Passagem','Seguro','Banco','Chip','Transporte','Primeiros dias'].map(x=><label className="check-item" key={x}><input type="checkbox"/> {x}</label>)}</div></section></div>
}
